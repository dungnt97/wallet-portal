# Admin API base URL (used by gen:api-types script)
# Default: http://localhost:3001 (proxied via Vite /api → :3001)
ADMIN_API_URL=http://localhost:3001

# Vite dev server port override (default: 5173)
# VITE_PORT=5173

# ── Chain RPC endpoints ───────────────────────────────────────────────────────
VITE_BNB_TESTNET_RPC=https://data-seed-prebsc-1-s1.bnbchain.org:8545
VITE_SOLANA_DEVNET_RPC=https://api.devnet.solana.com

# ── WalletConnect ─────────────────────────────────────────────────────────────
# Get project ID from https://cloud.walletconnect.com
# Required for WalletConnect connector (also covers Ledger Connect Kit path)
VITE_WALLETCONNECT_PROJECT_ID=get-from-cloud.walletconnect.com

# ── Multisig contract addresses (filled after Phase 02 deploy) ────────────────
VITE_SAFE_ADDRESS_BNB_TESTNET=
VITE_SQUADS_MULTISIG_PDA_DEVNET=

# ── Sentry (optional) ────────────────────────────────────────────────────────
# Leave unset to disable Sentry entirely (no-op, zero overhead).
# VITE_SENTRY_DSN=https://xxx@sentry.io/yyy
# Optional: set to your release tag for source-map attribution.
# VITE_RELEASE=1.0.0

# ── Multi-environment picker (optional) ──────────────────────────────────────
# JSON array of {name, apiUrl} profiles. When set, the env-picker pill is shown
# in the shell and the API client switches base URL on selection.
# Leave unset to run in single-env mode (picker hidden, all requests via Vite proxy).
# VITE_ENV_PROFILES=[{"name":"staging","apiUrl":"https://staging.api.example.com"},{"name":"prod","apiUrl":"https://api.example.com"}]
